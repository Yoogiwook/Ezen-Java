1. 인스턴스 변수, 메소드는 개체가 생성된어야 사용 가능.
2. 인스턴스 변수 및 메소드는 가용 가능 대상이 인스턴스 변수 인스턴스 메소드와
   클래스변수, 클래스 메소드 접근가능
3. 클래스 변수와 클래스 메소드는 객체 생성 전 method area 영역에 생성됨, 따라서 클래스 변수 메소드는
   생성되지 않은 인스턴스 변수, 인스턴스 메소드 접근 불가